<idea-plugin>
    <id>com.rixit.claude.chat</id>
    <name>Claude AI Assistant</name>
    <vendor email="info@rixitsolutions.be" url="https://rixitsolutions.be">Rix IT Solutions</vendor>

    <description><![CDATA[
        Chat with Claude from inside Rider.
        <ul>
            <li>Tool window with conversation history</li>
            <li>Send the current file or selection as context</li>
            <li>Cursor position and file path are auto-attached</li>
            <li>Model and API key configured in Settings &rarr; Tools &rarr; Claude AI Assistant</li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.5.2</h3>
        <ul><li>"Close this chat" entry in the hamburger menu - always available, even when JetBrains hides the tab strip (which it does whenever only one chat tab is open).</li></ul>
        <h3>0.5.1</h3>
        <ul><li>Attach toggle buttons now paint with an accent-blue background when armed, so the on/off state is obvious in any theme.</li></ul>
        <h3>0.5.0</h3>
        <ul>
            <li>Compact toolbar: attach toggles (sticky, with on/off visual state) on the left, "more options" hamburger on the right.</li>
            <li>Model selector, agent-mode toggle, and Clear conversation now live in the hamburger menu (with a Custom... entry for free-form model strings).</li>
            <li>Chat font set to Segoe UI Variable (falls back to Segoe UI / sans-serif when unavailable).</li>
        </ul>
        <h3>0.4.0</h3>
        <ul><li>Middle-click on a chat tab header to close it.</li></ul>
        <h3>0.3.2</h3>
        <ul><li>Added CLAUDE.md (project working agreement: commit / version / safety rules).</li></ul>
        <h3>0.3.1</h3>
        <ul>
            <li>Build script auto-refreshes plugin.zip on every gradle buildPlugin.</li>
            <li>New auto-build.bat / auto-build.ps1: run once, the plugin rebuilds on every file edit.</li>
        </ul>
        <h3>0.3.0</h3>
        <ul>
            <li>Renamed to "Claude AI Assistant".</li>
        </ul>
        <h3>0.2.0</h3>
        <ul>
            <li>Agent mode: Claude can read, edit, and create files in the project (per-write confirmation with diff preview).</li>
            <li>Auto-approve writes for 5min / 30min / 1hr / until the chat is closed.</li>
            <li>Multiple independent chat tabs via the "+" button.</li>
            <li>Per-chat model selector with sticky "last used" memory.</li>
            <li>Reply streaming.</li>
        </ul>
        <h3>0.1.0</h3>
        <ul><li>Initial release: chat tool window, file/selection attach, configurable model.</li></ul>
    ]]></change-notes>

    <!-- Works in any JetBrains IDE built on the platform (Rider included). -->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="ClaudeChat"
                    anchor="right"
                    factoryClass="com.rixit.claude.ui.ClaudeChatToolWindowFactory"
                    icon="AllIcons.Toolwindows.ToolWindowMessages"/>

        <applicationConfigurable parentId="tools"
                                 instance="com.rixit.claude.settings.ClaudeSettingsConfigurable"
                                 id="com.rixit.claude.settings"
                                 displayName="Claude AI Assistant"/>

        <applicationService serviceImplementation="com.rixit.claude.settings.ClaudeSettings"/>
    </extensions>

    <actions>
        