<idea-plugin>
    <id>com.rixit.claude.chat</id>
    <name>Claude AI Assistant</name>
    <vendor email="info@rixitsolutions.be" url="https://rixitsolutions.be">Rix IT Solutions</vendor>

    <description><![CDATA[
        Chat with Claude from inside Rider.
        <ul>
            <li>Tool window with conversation history</li>
            <li>Send the current file or selection as context</li>
            <li>Cursor position and file path are auto-attached</li>
            <li>Model and API key configured in Settings &rarr; Tools &rarr; Claude AI Assistant</li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.8.1</h3>
        <ul><li>Middle-click tab close now handles modern JBTabs - the tab label's getInfo()-&gt;TabInfo-&gt;getObject() path (used by Rider 2024.x+/2025.x) in addition to the older ContentTabLabel.getContent().</li></ul>
        <h3>0.8.0</h3>
        <ul><li>Agent mode is now ON by default in new chats. Claude can read, edit, and create files in the project right away. Every write still requires your approval with diff preview (toggle agent mode off in the hamburger menu for plain streaming chat).</li></ul>
        <h3>0.7.5</h3>
        <ul><li>Middle-click tab close now duck-types on the tab label's getContent() method rather than matching ContentTabLabel by name, so it works on Rider builds where the tab label class is named differently. (You can also close a chat via the hamburger -> "Close this chat" at any time.)</li></ul>
        <h3>0.7.4</h3>
        <ul><li>Image paste now also accepts file-list clipboard contents (e.g. images copied from Explorer) and image files dropped onto the chat input. Previously only the AWT imageFlavor path worked, which many Windows screenshots / file-copy operations don't use.</li></ul>
        <h3>0.7.3</h3>
        <ul><li>Fix NPE in AccentToggleButton.getForeground during initialization that prevented the tool window factory from constructing the chat panel, which is why everyone since 0.5.x was seeing "Nothing to show". The bug was a Kotlin non-null return type on an overridden Swing method that the L&amp;F's installColors calls before subclass fields are set.</li></ul>
        <h3>0.7.2</h3>
        <ul><li>Ctrl+Alt+K (or Tools -> Open Claude AI Assistant) is now a recovery action: opens the tool window AND seeds a fresh chat tab if it's empty. Works even when the tool window factory hasn't run for some reason.</li></ul>
        <h3>0.7.1</h3>
        <ul><li>Startup activity re-seeds the tool window with "Chat 1" if it's ever shown empty (e.g. after a plugin update from an older version left it in the 0-contents state). Factory also no longer double-seeds when it does run.</li></ul>
        <h3>0.7.0</h3>
        <ul>
            <li>Paste images (Ctrl+V) or drag-and-drop them into the chat input to send screenshots to Claude.</li>
            <li>Pending images appear as thumbnails above the input box; each has a small X to remove it before sending.</li>
        </ul>
        <h3>0.6.0</h3>
        <ul><li>Build now emits updatePlugins.xml next to plugin.zip. Add the file:// URL to Rider's plugin repositories once and Rider will auto-update the plugin on startup whenever a new version is built.</li></ul>
        <h3>0.5.3</h3>
        <ul><li>Closing the last chat now auto-spawns a fresh empty chat, so the tool window can't get stuck in the "Nothing to show" state.</li></ul>
        <h3>0.5.2</h3>
        <ul><li>"Close this chat" entr