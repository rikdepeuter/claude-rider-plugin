<idea-plugin>
    <id>com.rixit.claude.chat</id>
    <name>Claude AI Assistant</name>
    <vendor email="info@rixitsolutions.be" url="https://rixitsolutions.be">Rix IT Solutions</vendor>

    <description><![CDATA[
        Chat with Claude from inside Rider.
        <ul>
            <li>Tool window with conversation history</li>
            <li>Send the current file or selection as context</li>
            <li>Cursor position and file path are auto-attached</li>
            <li>Model and API key configured in Settings &rarr; Tools &rarr; Claude AI Assistant</li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.11.0</h3>
        <ul>
            <li>Bottom controls consolidated into a single tight row: + dropdown, Current file toggle, Selection toggle on the left; Plan toggle + hamburger on the right. Smaller font and tighter margins make the row noticeably more compact.</li>
            <li>"Agent" toggle replaced with "Plan" toggle (inverse semantics). Default off, meaning agent mode is on - Claude can read/edit files with the usual per-write confirmation. Toggle Plan on for read-only conversations.</li>
        </ul>
        <h3>0.10.0</h3>
        <ul>
            <li>Chat input redesign. Text input is now at the top. Below it: pending thumbnails / file chips, then a row of small inline attach toggles ("Current file", "Selection"), then a row with a new "+" dropdown (Browse files... + the currently-open editor files), the "Agent" toggle (default on), and the hamburger menu on the far right.</li>
            <li>Agent toggle is a first-class top-level button now (blue when armed) instead of a hidden checkbox in the menu.</li>
            <li>The "+" dropdown's "Open editor files" section reads from FileEditorManager - one click attaches a file you already have open.</li>
        </ul>
        <h3>0.9.2</h3>
        <ul><li>Build fix: 'return@try' isn't valid Kotlin (try blocks aren't labeled scopes). Restructured tryExtractContent to wrap the body in try/catch with regular function-level returns.</li></ul>
        <h3>0.9.1</h3>
        <ul><li>Build fix: 0.9.0 didn't compile (protected Clipboard.contents access + a chained ?.let { return@try ... } that the Kotlin parser couldn't make sense of). Same feature set, now actually buildable.</li></ul>
        <h3>0.9.0</h3>
        <ul>
            <li>Attach arbitrary files to a message: via the hamburger menu's <i>Attach files...</i> entry, by drag-dropping onto the input box, or by Ctrl+V from the system clipboard.</li>
            <li>Pending file attachments now appear alongside image thumbnails above the input, each with a small X to remove it before sending.</li>
            <li>Ctrl+V is now bound directly on the input area. Image paste reliably reaches the plugin's handler, instead of being swallowed by Rider's IDE-level paste action.</li>
            <li>Binary non-image files are rejected with a chat warning instead of silently sending garbage to the model.</li>
        </ul>
        <h3>0.8.1</h3>
        <ul><li>Middle-click tab close now handles modern JBTabs - the tab label's getInfo()-&gt;TabInfo-&gt;getObject() path (used by Rider 2024.x+/2025.x) in addition to the older ContentTabLabel.getContent().</li></ul>
        <h3>0.8.0</h3>
        <ul><li>Agent mode is now ON by default in new chats. Claude can read, edit, and create files in the project right away. Every write still requires your approval with diff preview (toggle agent m