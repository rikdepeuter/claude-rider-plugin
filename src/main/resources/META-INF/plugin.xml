<idea-plugin>
    <id>com.rixit.claude.chat</id>
    <name>Claude AI Assistant</name>
    <vendor email="info@rixitsolutions.be" url="https://rixitsolutions.be">Rix IT Solutions</vendor>

    <description><![CDATA[
        Chat with Claude from inside Rider.
        <ul>
            <li>Tool window with conversation history</li>
            <li>Send the current file or selection as context</li>
            <li>Cursor position and file path are auto-attached</li>
            <li>Model and API key configured in Settings &rarr; Tools &rarr; Claude AI Assistant</li>
        </ul>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.16.0</h3>
        <ul>
            <li>Markdown rendering: bold (**text**), italic (*text* / _text_), headings (#, ##, ###), bullet lists (-, *), numbered lists (1.), inline code (`), fenced code blocks (```). Previously only code was styled.</li>
            <li>Pasted images now show in the user bubble as a real preview thumbnail. We write the thumbnail to a temp file and reference it via file:// in the transcript HTML, because JTextPane's HTML loader doesn't reliably resolve data: URIs.</li>
            <li>On stream error mid-response, whatever partial text arrived before the failure is now preserved as the assistant turn (with the error message appended in red), instead of being dropped.</li>
        </ul>
        <h3>0.15.0</h3>
        <ul>
            <li>When a streaming response ends abnormally (anything other than the normal "end_turn"), the transcript now shows a small amber note explaining why - typically "max_tokens" (response was truncated because the per-message token cap was hit) or "refusal" (the model declined). For max_tokens it links to the Max tokens setting.</li>
            <li>Default Max tokens raised from 4096 to 8192. Long replies and big agent transcripts shouldn't get cut off as often. Existing users keep their saved value; this only affects fresh installs.</li>
        </ul>
        <h3>0.14.2</h3>
        <ul><li>Build fix: stopButton declaration referenced iconInsets before it was initialized. Kotlin initializes properties in source order, so iconInsets had to move above the buttons that use it.</li></ul>
        <h3>0.14.1</h3>
        <ul><li>Toggle buttons (Current file / Selection / Plan) get an explicit empty border so the L&amp;F's default ButtonBorder no longer reserves padding even when isBorderPainted is false. Plan in particular is noticeably tighter now.</li></ul>
        <h3>0.14.0</h3>
        <ul>
            <li>Text input now spans the full panel width. Send + Stop moved to the action row's far right.</li>
            <li>Action row consolidated: + and attach toggles on the left; Plan, hamburger, Stop (icon, visible only while busy), and Send on the right.</li>
            <li>Buttons tightened: + and hamburger and Stop are zero-padding 20x20 icons; toggle buttons (Current file / Selection / Plan) lose vertical padding and use 6px horizontal padding.</li>
            <li>Stop is now an icon (AllIcons.Actions.Suspend) instead of a text button.</li>
        </ul>
        <h3>0.13.0</h3>
        <ul>
            <li>Streaming responses no longer choke the EDT. Deltas accumulate into a buffer that's flushed to the JTextPane via a 50ms Timer, instead of re-rendering the entire HTML transcript on every chunk. Long replies appear smoothly now and the Send button no longer gets stuck on "Sending..." behind a backlog of paint events.</li>
            